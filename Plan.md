# Plan：血壓日誌主題切換與偏好保存

## 需求概述
本專案為單頁式血壓紀錄應用，目標是在首頁上方新增 dark/light 兩種佈景主題切換開關，並讓使用者的選擇被儲存到瀏覽器本機儲存空間中，讓重新整理或重新開啟頁面時仍保持上一次的佈景主題設定。

## 目標
- 提供 dark/light 兩種佈景主題
- 在首頁上方加入主題切換開關
- 使用者選擇後保存到 localStorage
- 首次進入頁面時使用預設主題（dark）
- 確保兩種主題的文字、背景、按鈕與表單都具備良好的可讀性與對比度

## 設計範圍
- 本次僅處理目前簡單頁面應用的主題切換功能
- 儲存方式採用瀏覽器本機儲存，不涉及後端伺服器或帳號同步
- 主題切換會套用到目前頁面中的全域樣式與主要元件
- 若後續擴充其他頁面，可沿用同一套主題系統

## 實作步驟

### 1. 檢視現有頁面結構與樣式
- 確認首頁上方標題列或工具列的合適位置
- 檢查目前的 CSS 是否已存在可抽離的色值與樣式系統
- 找出可放置主題切換按鈕的最小改動位置

### 2. 抽離主題色票
- 將既有配色拆成 light 與 dark 兩套色票
- 以 CSS 變數方式管理背景、文字、邊框、卡片、按鈕、標籤等色彩
- 讓整個頁面使用變數而非寫死顏色，方便切換主題

### 3. 新增主題切換 UI
- 在首頁上方新增切換開關
- 採用「文字 + 圖示按鈕」風格
- 提供明確的目前狀態顯示，方便使用者理解現在為 dark 或 light

### 4. 實作主題狀態管理
- 定義一個主題儲存鍵，例如：`theme-preference`
- 在頁面載入時讀取 localStorage
- 若無資料，使用 dark 作為預設值
- 若資料無效，回退為安全預設值

### 5. 實作切換邏輯
- 點擊切換按鈕時，更新目前主題狀態
- 將主題狀態同步到 HTML 根元素或全域資料屬性
- 立即將設定寫回 localStorage
- 確保重新整理後仍維持相同主題

### 6. 驗證與修正
- 確認首次進入頁面為 dark
- 切換為 light 後重新整理仍保持 light
- 確認表單、按鈕、歷史紀錄與空白狀態在 dark 模式下可讀性良好
- 確認 mobile 與 desktop 版面皆正常
- 檢查是否有不一致的樣式或低對比問題

## 主要變更檔案
- `blood-app/index.html`：新增主題切換按鈕的位置與結構
- `blood-app/style.css`：新增 light/dark 兩種主題變數與相關樣式覆蓋
- `blood-app/app.js`：新增主題初始化、切換、讀取與保存邏輯

## 預期結果
完成後，使用者可以在首頁上方輕鬆切換 dark/light 兩種佈景主題，且這個偏好會被保存到瀏覽器，讓每次回到頁面時都能看到上一個選擇的主題。

## 風險與注意事項
- 若 CSS 變數未完全覆蓋所有元件，可能出現深色模式下部份區塊仍為亮色
- 若 `localStorage` 讀取失敗，需有 fallback logic，避免頁面錯誤
- 需避免把主題邏輯與紀錄資料儲存混在同一個鍵名，免得衝突

## 驗證 checklist
- [ ] 首頁上方顯示主題切換開關
- [ ] 預設為 dark 模式
- [ ] 切換後可即時更新畫面
- [ ] 重新整理後仍保持上一個主題
- [ ] light/dark 模式下皆可閱讀
- [ ] 無資料或無效資料時可正常回到預設

## 結論
此需求的實作方式屬於前端單一頁面應用的典型作法，最適合採用 CSS 變數與 localStorage 的組合，以最小成本實現主題切換與偏好保存，同時保留擴充性。
